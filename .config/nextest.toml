[profile.unit]
default-filter = "kind(lib)"
fail-fast = false

[profile.property]
default-filter = "binary(~property)"
fail-fast = false

[profile.acceptance]
default-filter = "binary(~acceptance)"
fail-fast = false

[profile.mutation]
default-filter = "binary(~mutation_hardening) | kind(lib)"
fail-fast = false
